services:
  backend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - BM_DB_PATH=/data/bookmarks.db
    volumes:
      - bm-data:/data
    restart: unless-stopped

volumes:
  bm-data:
